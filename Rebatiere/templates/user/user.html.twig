<!DOCTYPE html>

{# Voir plus de layouts sur https://useaxentix.com/docs/general/layouts/ #}
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {# layout axentis #}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/axentix@2.4.0/dist/axentix.min.css">

        {% block stylesheets %}
        <style> 
        .left-align {
          margin-right: auto !important;
        }
        </style>
        {% endblock %}
    </head>

    <body class="layout-sidenav">

    <header>
      <nav class="navbar shadow-1 primary">
        <div class="navbar-menu ml-auto">
          <a class="navbar-link left-align" href="{{ path('app_home') }} ">Calendrier</a>
          <a class="navbar-link" href="{{ path('user.edit', {'id': currentUser.id}) }} ">Mon profil</a>
          <a class="navbar-link" href=" {{ path('user.reservation.edit', {'id': currentUser.id}) }} ">Mes réservations</a>
        </div>
      </nav>
    </header>

    {# Flash Messages #}
    {# {% include 'partials/flash.html.twig' %} #}
   
    <main class="mainContainer">
    <main class="mainOverlay">
      
    {% block body %}
    {# User edit et reservations #}

        {% block javascripts %}
        {# Script pour la modal flash #}
          <script>
          document.addEventListener("DOMContentLoaded", function () {
              setTimeout(function () {
                  document.querySelectorAll('.alert-sucess').forEach(function(flash) {
                      flash.classList.add('hide');
                      setTimeout(() => flash.remove(), 500); // Supprime l'élément après l'animation
                  });
              }, 3000);
            });
          </script>
        {% endblock %}
    {% endblock %}
    </main>
    </main>

    <footer class="footer primary">
     
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/axentix@2.4.0/dist/axentix.min.js"></script>

    </body>
    
</html>
